name: Tests

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Setup cache
        uses: actions/cache@v6
        with:
          path: |
            .dart_tool/
            build/
            pubspec.lock
          key: ${{ runner.OS }}-perfect_freehand-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.OS }}-perfect_freehand-

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
          cache-key: 'flutter-:os:-:channel:-:version:-:arch:'

      - run: flutter pub get

      - name: Run data-driven-fixes tests
        working-directory: test_fixes
        run: dart fix --compare-to-golden

      - name: Run tests
        run: flutter test
